import Header from '@/components/Header';
import styled from 'styled-components';

const Main = () => {
  return (
    <MainStyle>
      <Header />
    </MainStyle>
  );
};

const MainStyle = styled.div`
  
`;

export default Main;
