import styled from 'styled-components';

const Error = () => {
  return (
    <ErrorStyle>
      <div>error</div>
    </ErrorStyle>
  );
};

const ErrorStyle = styled.div`
  
`;

export default Error;
